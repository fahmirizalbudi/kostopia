export enum TransactionPurpose {
  New = "new",
  Renewal = "renewal",
}

export enum TransactionStatus {
  Pending = "pending",
  Success = "success",
  Rejected = "rejected",
}

export const NULL_PROOF = ""